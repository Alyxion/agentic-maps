"""Self-contained export documents (no framework imports, no network).

`map_page` builds the embeddable route page an MCP client displays as its
own artifact — see docs/mcp.md "Copyright containment".
"""
